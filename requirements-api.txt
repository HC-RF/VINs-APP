# FastAPI deployment path: `uvicorn app.main:app`.
# Not used by Streamlit Cloud.
-r requirements.txt

# FastAPI must be >= 0.116: earlier releases cap starlette below 1.0, which
# Streamlit 1.62+ requires. Pinning below that breaks at import with
# "Router.__init__() got an unexpected keyword argument 'on_startup'".
fastapi>=0.116,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1

# PostgreSQL driver. Required in any real deployment; see the note in
# requirements.txt about wheel availability.
psycopg[binary]>=3.1,<4
